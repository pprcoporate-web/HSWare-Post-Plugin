{
  "name": "hsware-post-mcp-worker",
  "version": "3.2.2",
  "private": true,
  "type": "module",
  "description": "HSWare Post fast-default MCP server for Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/hsware.js && node --test",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
